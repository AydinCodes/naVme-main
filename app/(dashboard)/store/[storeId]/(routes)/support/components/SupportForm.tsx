interface SupportFormProps {
    
}

const SupportForm: React.FC<SupportFormProps> = ({  }) => {
    return (
        <div>
            Support Form
        </div>
    );
};

export default SupportForm;