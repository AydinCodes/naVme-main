interface SettingsFormProps {
    
}

const SettingsForm: React.FC<SettingsFormProps> = ({  }) => {
    return (
        <div>
            Settings Form
        </div>
    );
};

export default SettingsForm;